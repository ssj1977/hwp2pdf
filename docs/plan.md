# Plan

## 목표

- CLI 검증 과정에서 발생한 문제와 해결 절차를 문서화한다.
- `test.hwp` 전체 포맷 변환 명령을 문서에 남긴다.

## 단계

1. README에 상태/문서 안내 맵 반영
2. 기술 문서(`PRD`, `architecture`, `implementation`) 작성
3. 장애 기록(`findings`, `decision`) 작성
4. 진행상태/이력(`progress`, `prompt_history`) 기록
